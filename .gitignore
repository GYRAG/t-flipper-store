build/
build_t_embed/
build_test/
build_s3/
.cache/
.idea/
__pycache__/
sdkconfig.old
.DS_Store
build_waveshare_c6/
.vscode/settings.json

# Local-dev tooling (not for upstream)
_flash_helper.py

# Managed components are pulled by IDF component manager from idf_component.yml
# + dependencies.lock; the source-of-truth is those two files.
managed_components/

# Local development snapshots and generated release artifacts
.backups/
*.before-*
*.after-*
*.bak
*.orig
*.rej
*~
releases/*.zip
*-backup-*
*.backup-*
# graphify knowledge-graph output + local interpreter pointer (local only)
graphify-out/
.graphify_*

# Local web-flasher staging (generated bins copied from build_t_embed/)
release/

/tmp/

# store test FAPs are build artifacts (rebuilt from applications_user/)
store/faps/
# store catalog is generated by tools/make_catalog.py from store/apps.json + the built FAPs
store/catalog.json
# flat form of the same catalog, for the on-device WiFi store
store/catalog.txt

# internal working notes — keep local, don't publish
BUGS.md
FUTURE_PLANS.md
HANDOFF.md
